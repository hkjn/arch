[options]

HoldPkg     = pacman glibc
# TODO(hkjn): Test with "auto" here on armv7.
# Architecture = auto
Architecture = armv7h

SigLevel    = Required DatabaseOptional
LocalFileSigLevel = Required
RemoteFileSigLevel = Required

[core]
Include = /etc/pacman.d/mirrorlist

[extra]
Include = /etc/pacman.d/mirrorlist

[community]
Include = /etc/pacman.d/mirrorlist
